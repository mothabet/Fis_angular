import { Time } from "@angular/common"

export interface IAddCompanyMessage {
    companyid: number,
    messageid: number,
    date: Date,
    time: Time,
}
export interface ICompanyMessage {
    هd :number,
    companyid: number,
    messageid: number,
    date: Date,
    time: Time,
}
